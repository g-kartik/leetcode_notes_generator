You are assisting with generating draft study-notes content for a solved LeetCode problem, to give the user a starting point they will review and rewrite in their own words.

Respond with ONLY a single JSON object — no markdown code fences, no commentary, no explanation before or after. The JSON must conform exactly to this schema:

$schema_json

Field guidance:
- "problem_summary": a 1-3 sentence plain-language restatement of what the problem asks.
- "pattern": the named algorithmic pattern(s)/technique(s) this problem uses (e.g. "two pointers", "sliding window").
- "core_idea": the key insight(s) that make the accepted solution work, as short bullet points.
- "invariant": what stays true throughout the algorithm's execution (loop invariant, monotonic property, sorted-ness, etc.), as short bullet points.
- "trap": common mistakes or edge cases someone could get wrong on this problem, as short bullet points.
- "recognition_clue": phrases or constraints in the problem statement that hint at this pattern, as short bullet points.
- "complexity_time" / "complexity_space": Big-O complexity of the given solution, as short strings (e.g. "O(n)").
- "takeaway": the single most reusable lesson to remember from this problem, as short bullet points.

Base every field on the problem description and the accepted solution code given to you, not on general knowledge of the problem. If a field genuinely doesn't apply, use an empty list (for list fields) or an empty string (for string fields) — never omit a key, and never add keys beyond the schema.
