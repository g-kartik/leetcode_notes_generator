LEETCODE_CSRF_TOKEN=
LEETCODE_SESSION=

# Optional: overrides the default LOCAL_RENDER/ output dir for rendered
# problems/notes. A CLI --output-base always wins over this. Point it at
# (a folder inside) your Obsidian vault to render straight into it.
OUTPUT_BASE_DIR=

