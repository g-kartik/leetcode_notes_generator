{
  "prefills": {
    "container-with-most-water": [
      {
        "generated_at": "2026-08-25T16:47:57.260504",
        "provider": "claude_code",
        "content": {
          "problem_summary": "Given n vertical lines at positions 0..n-1 with heights given by the array, choose two lines that together with the x-axis form a container, and return the maximum amount of water it can hold (area = width between the lines times the shorter line's height).",
          "pattern": [
            "Two Pointers",
            "Greedy"
          ],
          "core_idea": [
            "Start with the widest possible container (leftmost and rightmost lines) and shrink inward, since width can only decrease as pointers move.",
            "At each step compute area as (right - left) * min(height[left], height[right]) and track the max seen.",
            "Always move the pointer at the shorter line inward, because keeping the shorter line and reducing width can never increase area — only replacing the shorter line offers a chance at a taller bound.",
            "Discarding the taller line while keeping the shorter one is provably suboptimal, so the greedy pointer move never eliminates the true optimal answer."
          ],
          "invariant": [
            "The current [left, right] window always contains the maximum-area pair found so far or a pair that could still beat it — no optimal pair lies outside the shrinking window.",
            "Width (right - left) strictly decreases each iteration.",
            "max_water is monotonically non-decreasing throughout the loop."
          ],
          "trap": [
            "Moving both pointers or moving the taller pointer instead of the shorter one breaks the greedy correctness argument.",
            "Off-by-one on width calculation (should be right - left, not right - left + 1, since lines have zero width).",
            "Forgetting that height[i] can be 0, which still needs to be handled correctly (area becomes 0 for that pairing).",
            "Assuming the brute-force O(n^2) all-pairs check is required; two pointers reduce it to O(n)."
          ],
          "recognition_clue": [
            "Asks for max area/container formed by two elements in an array with the x-axis, implying width * min-height formula.",
            "Array of heights with an implicit index-based width — classic setup for two pointers from both ends.",
            "Tags explicitly include Two Pointers and Greedy."
          ],
          "complexity_time": "O(n)",
          "complexity_space": "O(1)",
          "takeaway": [
            "When width shrinks monotonically and area depends on min(height), greedily move the pointer at the smaller boundary inward — it's the only move that could possibly improve the result."
          ]
        }
      },
      {
        "generated_at": "2026-08-25T16:48:21.494029",
        "provider": "claude_code",
        "content": {
          "problem_summary": "Given heights of n vertical lines at positions 0..n-1, pick two lines that together with the x-axis form a container, and find the maximum possible water area (area = width × min of the two heights). The container cannot be slanted, so height is bounded by the shorter line.",
          "pattern": [
            "Two Pointers",
            "Greedy"
          ],
          "core_idea": [
            "Start with the widest possible container (leftmost and rightmost lines) and shrink inward.",
            "Area is limited by the shorter of the two lines, so moving the pointer at the taller line can never increase area (width shrinks, height stays capped by the same or smaller min), but moving the pointer at the shorter line might find a taller line and increase area.",
            "Always move the pointer pointing to the shorter line inward, since keeping it can only produce equal-or-worse results with any partner.",
            "Track the maximum area seen across all pointer positions visited."
          ],
          "invariant": [
            "At every step, left < right, and all pairs outside the current [left, right] window have already been considered or are provably no better than what's tracked.",
            "The pointer movement guarantees the optimal container is never skipped, because the untried pair with the shorter line's index is dominated by the current pair."
          ],
          "trap": [
            "Forgetting that area is width × min(height[left], height[right]), not max — using the taller line overestimates capacity.",
            "Moving the wrong pointer (taller one) or moving both pointers at once can skip over the true maximum.",
            "Off-by-one on width calculation: width is right - left (number of gaps), not right - left + 1.",
            "Not handling equal heights consistently (moving either pointer works when heights are equal, but must move at least one).",
            "Assuming a brute-force O(n^2) check of all pairs is required — greedy two-pointer reduces it to O(n)."
          ],
          "recognition_clue": [
            "Array represents heights/values indexed by position, and you need to choose two indices to maximize an area/volume/capacity metric.",
            "The answer is monotonic in the sense that shrinking the window from both ends while making a greedy local choice doesn't lose the optimal pair.",
            "n up to 1e5 suggests an O(n) or O(n log n) solution is expected, ruling out brute force O(n^2)."
          ],
          "complexity_time": "O(n)",
          "complexity_space": "O(1)",
          "takeaway": [
            "When the metric is bounded by the smaller of two chosen endpoints and width shrinks as pointers move inward, greedily discard the pointer at the smaller endpoint — it can never be part of a better solution than the current one."
          ]
        }
      },
      {
        "generated_at": "2026-08-25T17:09:14.985638",
        "provider": "claude_code",
        "content": {
          "problem_summary": "Given n vertical lines at positions 0..n-1 with heights given by the array, find two lines that together with the x-axis form a container holding the maximum amount of water. The area is width (distance between indices) times the shorter of the two heights.",
          "pattern": [
            "Two Pointers",
            "Greedy"
          ],
          "core_idea": [
            "Start with the widest possible container (leftmost and rightmost lines) and shrink inward.",
            "Area is limited by the shorter of the two lines, so moving the pointer at the taller line can never increase area (width only decreases and height is capped by the same or smaller min); only moving the shorter line's pointer has a chance to find a taller line and possibly increase area.",
            "At each step compute width * min(height[left], height[right]) and track the maximum seen.",
            "Greedily discard the shorter line at each step since it can never be part of a better solution than what's already been considered at this width."
          ],
          "invariant": [
            "At every iteration, all containers wider than the current [left, right] window have already been considered (since left and right start at the extremes and only move inward).",
            "The pointer that gets moved always points at the line with the smaller (or equal) height, ensuring no potentially-better container is skipped."
          ],
          "trap": [
            "Moving the taller pointer instead of the shorter one discards the correct candidate and can skip the optimal answer.",
            "Off-by-one in width calculation: width should be right - left, not right - left + 1 (0-indexed line positions, not counts).",
            "Forgetting to update max_water before moving pointers.",
            "Ties in height (height[left] == height[right]) — either pointer can move, but must move exactly one, not both.",
            "Assuming a brute-force O(n^2) check of all pairs is required; the greedy two-pointer approach is what achieves O(n)."
          ],
          "recognition_clue": [
            "Asks for a maximum area/volume formed by two boundaries with the array representing heights.",
            "Constraint hints at needing better than O(n^2) (n up to 1e5).",
            "Problem structure involves choosing two indices to maximize width * min(height) — a classic setup for inward-moving two pointers with a greedy elimination argument."
          ],
          "complexity_time": "O(n)",
          "complexity_space": "O(1)",
          "takeaway": [
            "When maximizing width * min(height) between two boundaries, use two pointers starting at the extremes and always move the pointer at the smaller height inward — this greedy elimination is provably safe and avoids O(n^2) brute force."
          ]
        }
      }
    ],
    "two-sum": [
      {
        "generated_at": "2026-08-25T16:56:28.704401",
        "provider": "claude_code",
        "content": {
          "problem_summary": "Given an array of integers and a target sum, find the indices of the two distinct elements that add up to the target, assuming exactly one solution exists.",
          "pattern": [
            "Hash Table",
            "One-pass complement lookup"
          ],
          "core_idea": [
            "For each number, compute its complement (target - num) and check if that complement was already seen.",
            "Use a hash map to store previously seen numbers mapped to their indices, enabling O(1) lookup.",
            "As soon as a complement match is found, return the stored index and the current index — no need to scan further."
          ],
          "invariant": [
            "At the time index idx is processed, memo contains exactly the numbers (and their indices) from nums[0..idx-1].",
            "Each element is added to memo only after checking it against previously seen elements, so no element is paired with itself."
          ],
          "trap": [
            "Adding the current number to memo before checking for its complement would incorrectly allow using the same element twice.",
            "Must store indices, not just values, since the answer requires indices not the numbers themselves.",
            "Duplicate values (e.g. [3,3], target=6) work correctly here because the first 3 is stored in memo before the second 3 is processed.",
            "Brute-force nested loop gives O(n^2); the hash map approach is needed to meet the O(n) follow-up requirement."
          ],
          "recognition_clue": [
            "Need to find a pair of elements satisfying a sum condition — classic complement-lookup setup.",
            "'Exactly one solution exists' hints that a single pass with early return suffices.",
            "Follow-up explicitly asks for better than O(n^2), pointing toward hashing instead of brute force."
          ],
          "complexity_time": "O(n)",
          "complexity_space": "O(n)",
          "takeaway": [
            "When searching for pairs that satisfy a sum/difference condition, trade space for time by hashing seen elements and checking complements in a single pass."
          ]
        }
      }
    ],
    "binary-search": [
      {
        "generated_at": "2026-08-25T20:33:17.760854",
        "provider": "claude_code",
        "content": {
          "problem_summary": "Given a sorted (ascending) array of unique integers and a target value, find the index of the target using an algorithm that runs in O(log n) time, or return -1 if it's not present.",
          "pattern": [
            "Binary Search"
          ],
          "core_idea": [
            "Repeatedly halve the search space by comparing the target to the middle element.",
            "If nums[mid] equals target, return mid immediately.",
            "If nums[mid] < target, the target must be in the right half, so move left to mid+1.",
            "If nums[mid] > target, the target must be in the left half, so move right to mid-1."
          ],
          "invariant": [
            "If the target exists in nums, it always lies within the current [left, right] window.",
            "The search window [left, right] shrinks by at least half each iteration.",
            "nums remains sorted throughout (never mutated), which is what makes discarding half the array valid."
          ],
          "trap": [
            "Using mid = (left + right) // 2 can overflow in languages with fixed-width integers; left + (right - left) // 2 avoids that (not an issue in Python but good habit).",
            "Off-by-one errors in updating left/right (must be mid+1 / mid-1, not mid, to guarantee termination).",
            "Loop condition must be left <= right (not <), otherwise a single-element window is skipped.",
            "Forgetting to return -1 when the loop exits without finding the target."
          ],
          "recognition_clue": [
            "Array is explicitly stated to be sorted in ascending order.",
            "Requirement of O(log n) runtime complexity rules out linear scan.",
            "All elements are unique, so there's exactly one valid index to find (no duplicate-handling complexity)."
          ],
          "complexity_time": "O(log n)",
          "complexity_space": "O(1)",
          "takeaway": [
            "Sortedness + a required sub-linear time bound is the classic signal to reach for binary search: use inclusive bounds (left <= right) and always move the excluded boundary past mid (mid+1/mid-1) to guarantee progress and correctness."
          ]
        }
      },
      {
        "generated_at": "2026-08-25T22:00:25.659673",
        "provider": "claude_code",
        "content": {
          "problem_summary": "Given a sorted (ascending) array of unique integers and a target value, find the index of the target in O(log n) time, or return -1 if it isn't present.",
          "pattern": [
            "Binary Search"
          ],
          "core_idea": [
            "Repeatedly halve the search space by comparing the target to the middle element.",
            "Because the array is sorted, the comparison at mid tells you which half can be discarded entirely.",
            "Use inclusive bounds [left, right] and shrink them until they cross or the target is found."
          ],
          "invariant": [
            "If the target exists in nums, it always lies within nums[left..right] at the start of each loop iteration.",
            "left and right only move inward (left increases, right decreases), so the search space is strictly shrinking each iteration."
          ],
          "trap": [
            "Using left + (right - left) // 2 instead of (left + right) // 2 avoids integer overflow in languages with fixed-width ints (less critical in Python but good habit).",
            "Loop condition must be left <= right (not <), otherwise the case where left == right (single remaining candidate) is skipped.",
            "Off-by-one errors when updating bounds: must set left = mid + 1 / right = mid - 1, not mid itself, or you risk an infinite loop.",
            "Returning -1 only after the loop ends, not prematurely inside it."
          ],
          "recognition_clue": [
            "Array is explicitly stated to be sorted in ascending order.",
            "Requirement to achieve O(log n) runtime complexity.",
            "All elements are unique, so there's exactly one valid index to find (no duplicate-handling complexity)."
          ],
          "complexity_time": "O(log n)",
          "complexity_space": "O(1)",
          "takeaway": [
            "Whenever an array is sorted and you need faster-than-linear search, think binary search: use inclusive [left, right] bounds and eliminate half the space each step based on a comparison at mid."
          ]
        }
      }
    ],
    "gas-station": [
      {
        "generated_at": "2026-08-25T20:39:38.481701",
        "provider": "claude_code",
        "content": {
          "problem_summary": "Given circular gas stations with gas[i] available and cost[i] to travel to the next station, find the unique starting index from which a car with unlimited tank capacity can complete the full circuit, or return -1 if impossible.",
          "pattern": [
            "Greedy",
            "Prefix sum / running total"
          ],
          "core_idea": [
            "If total gas >= total cost, a valid starting point is guaranteed to exist (and is unique).",
            "Track a running tank balance while scanning left to right; whenever it goes negative, no station in the range since the last reset could have been a valid start, so reset the candidate start to the next index and reset the tank to 0.",
            "The final surviving start index after one pass is the answer when total gas >= total cost."
          ],
          "invariant": [
            "current_gas represents the net gas balance accumulated since the last candidate start index.",
            "Once current_gas goes negative at index i, none of the stations between the previous start and i can reach past i, so they're all eliminated as candidates.",
            "total_gas - total_cost equals the tank balance at the end of a full circuit starting from index 0."
          ],
          "trap": [
            "Forgetting to check total_gas < total_cost first — without it you might return a start index that actually can't complete the loop.",
            "Resetting current_gas to 0 but forgetting to also update start = i + 1 (or vice versa) when the tank goes negative.",
            "Assuming you need to re-simulate from each candidate start (O(n^2)) instead of realizing a single pass suffices.",
            "Off-by-one on the wrap-around when validating the answer manually (circuit goes i -> i+1 ... back to start)."
          ],
          "recognition_clue": [
            "Circular route / wrap-around array structure.",
            "Problem guarantees a unique answer if one exists, hinting a single greedy pass can find it.",
            "Asks for feasibility of traversing a cycle under a running balance constraint (gas - cost), suggesting prefix-sum/greedy reasoning rather than brute force simulation."
          ],
          "complexity_time": "O(n)",
          "complexity_space": "O(1)",
          "takeaway": [
            "When a running sum starting from a fixed point goes negative, every point since the last reset is disqualified as a valid start — this lets a single linear scan find the unique feasible starting index instead of testing each candidate separately."
          ]
        }
      }
    ],
    "car-fleet": [
      {
        "generated_at": "2026-08-25T21:56:27.022173",
        "provider": "claude_code",
        "content": {
          "problem_summary": "Given cars at distinct positions moving toward a common target at given speeds, cars merge into a fleet when a faster car behind catches up to a slower one ahead (fleets travel at the slower speed and never separate). Count how many distinct fleets arrive at the target.",
          "pattern": [
            "Monotonic stack",
            "Sorting"
          ],
          "core_idea": [
            "Compute the time each car would take to reach target if unobstructed: (target - position) / speed.",
            "Process cars from closest-to-target to farthest (sort by position descending).",
            "A car forms a new fleet only if its arrival time is strictly greater than the max arrival time seen so far among cars ahead of it; otherwise it catches up and merges into the fleet ahead.",
            "Track a running maximum of arrival times; each time a new max is found, increment the fleet count."
          ],
          "invariant": [
            "Cars are processed in strictly decreasing order of starting position, so we always compare a car to fleets already 'ahead' of it.",
            "max_time always holds the largest arrival time among all fleets formed so far (i.e., the slowest/last-arriving fleet encountered from the target backward).",
            "A car with arrival time <= max_time can never separate from the fleet ahead since it would have to pass a slower car."
          ],
          "trap": [
            "Sorting must be by position, not by speed or index — zip(position, speed) then sort handles this correctly.",
            "Must iterate from the car closest to target backward (reverse after sorting by position ascending), not forward, since fleet formation depends on catching up to the car ahead.",
            "A car catching up exactly at the target still counts as joining the fleet, so the comparison must be strict (time > max_time forms a new fleet, time <= max_time merges).",
            "Single car input should still return 1 fleet — handled naturally since the first (farthest-from-target-in-reverse-order, i.e., closest) time is always > 0."
          ],
          "recognition_clue": [
            "'Cannot pass another car but can catch up and travel together' implies merging based on relative arrival times.",
            "Asking for count of groups arriving together at a fixed destination suggests comparing time-to-reach rather than raw positions/speeds.",
            "Only one array of positions/speeds combined with a single target distance hints at a single pass after sorting."
          ],
          "complexity_time": "O(n log n)",
          "complexity_space": "O(n)",
          "takeaway": [
            "When objects moving toward a common endpoint can merge but not pass each other, convert to arrival times and sweep from the endpoint backward, tracking a running maximum — a classic monotonic stack pattern in disguise."
          ]
        }
      }
    ],
    "maximum-sum-of-distinct-subarrays-with-length-k": [
      {
        "generated_at": "2026-08-26T12:34:12.519816",
        "provider": "claude_code",
        "content": {
          "problem_summary": "Given an array nums and integer k, find the maximum sum among all length-k contiguous subarrays whose elements are all distinct; return 0 if none qualify.",
          "pattern": [
            "Sliding window (fixed size)",
            "Two pointers with a hash set for duplicate tracking"
          ],
          "core_idea": [
            "Maintain a window of exactly k elements using a set to track distinctness and a running sum for the total.",
            "When the incoming element already exists in the window, shrink from the left (removing elements from the set and sum) until the duplicate is gone before adding the new element.",
            "After insertion, if the window exceeds size k, evict the leftmost element to keep the window size fixed at k.",
            "Only compare/update the result when the window size is exactly k, since that's the only valid state to check."
          ],
          "invariant": [
            "The set 'window' always contains exactly the elements currently between left and right (inclusive), with no duplicates.",
            "window_sum always equals the sum of the elements currently in 'window'.",
            "len(window) never exceeds k at the end of each iteration."
          ],
          "trap": [
            "Forgetting to remove duplicates from the left before adding the new right element leads to an incorrect set/sum state.",
            "Only updating the result when len(window) == k — checking at the wrong time (e.g., before the window reaches size k) undercounts valid windows.",
            "Not shrinking the window size back down to k after it grows to k+1, which would let the window drift out of the fixed-size constraint.",
            "Off-by-one errors in when 'left' is advanced relative to updating window_sum and the set.",
            "Returning something other than 0 when no valid window of distinct elements exists."
          ],
          "recognition_clue": [
            "Fixed subarray length k is specified, hinting at a fixed-size sliding window rather than variable-size.",
            "Requirement that all elements in the subarray be distinct suggests using a hash set to detect duplicates in the current window.",
            "Need for a running sum (subarray sum) suggests maintaining an incremental sum rather than recomputing per window.",
            "Large constraint (nums.length up to 1e5) rules out brute force O(n*k) and points to an O(n) sliding-window approach."
          ],
          "complexity_time": "O(n) — each element is added and removed from the window at most once across the traversal",
          "complexity_space": "O(k) — the set/window holds at most k distinct elements at a time",
          "takeaway": [
            "When a subarray must have a fixed length and a uniqueness constraint, combine a fixed-size sliding window with a hash set to enforce distinctness while incrementally tracking the sum for O(n) performance."
          ]
        }
      }
    ]
  }
}